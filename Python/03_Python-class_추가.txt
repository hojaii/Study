Python의 함수

특정처리를 하는 코드를 묶어놓은 코드집함

- 사용자 정의함수

사용자가 직접만들어 사용하는함수

- 내장함수

Python이 우리에게 기본적으로 제공하는 함수

id()	tuple()	dict()
print()	type()	set()
sorted()	list()	range()
sum()

내장함수는 많이 알수록 좋음

Python 함수로 인자를 호출할때 

숫자, 문자열, tuple >> immtable

list, dict >> mutable

상속

상위 class가 가진 특성을 물려받아 하위클래스를 정의하는 방법으로 기존클래스를 확장하여

새로운 클래스를 생성함

상속 사용시 superclass와 subclass가 강하게 결합되어 재사용성이 떨어지지만 이 문제점을

Interface를 이용하면 해결이 가능함
