Module

python의 module을 함수 를 모아 놓은 file을 지칭함
		 변수
		 class

python 프로그램에서 불러다가 사용할수있음

module을 사용하는이유 재사용
		       관리가 편해짐

module은 크게 2가지 C언어로 구축된 binary module >>속도를 높히기 위해
			            python 언어로 구현된 일반 module

import 키워드를 사용해서 module로 사용


Web

1. internet 
LAN들을 연결해서 전세계적인 network망을 만들어 놓은것 : Network Infra

2. Internet위에서 (Internet을 활용하기 위해서는 Program이 필요)동작하는
   Service => web service , p2p(torrent), email service, 

3. web service >> 기본적으로 c/s 구조 (client/server)

4. web server(program)
module을 이용해서 작성(django framework사용)

library
>단일기능을 구현해둔것(함수) 	 	
				둘다 프로그램을 하기위해 필요한 어느정도 완성된 코드
framework  
>library을 포함하고 전체 프로그램의 동작 방식까지 포함

기본용어

Web Server : Internet을 통해 web client로부터 요청이 들어오면 해당 request에 대한 web contents를 

client에게 다시 전달해주는 software
