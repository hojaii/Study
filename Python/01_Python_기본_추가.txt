Python 기본

파이선은 교육용 언어기 때문에 들여쓰기를 강조하기 위해서

공란 하나하나가 모두 의미를 가짐

셀 전체선택후 B키 : 현재셀 아래에 셀 생성
	      A키 : 현재셀 위쪽에 셀 생성
	      D키 두번 : 현재셀 삭제


Python의 데이터타입

Numeric

정수(int), 실수(float), 복소수(complex)

일반적인 프로그램언어에서는 연산이 같은데이터타입끼리 발생함

c= 3/4는 정수타입을 반환해야하므로 기본적인 언어에서는 0을 반환하는데

Python의 경우는 0.75가 출력이됨 (다른 데이터 타입이 출력)

연산자 % : 나머지
연산자 // : 몫
